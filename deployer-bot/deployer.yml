name: deployer

services:
  deployer:
    image: deployer-bot:v4
    container_name: deployer
    env_file:
      - .env
    networks:
      - devnet

networks:
  devnet:
    external: true
    name: devnet