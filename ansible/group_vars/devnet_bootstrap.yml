compose_jobs:
  - name: bootstrap
    work_dir: ~/devnet-bootstrap
    compose_file: bootstrap.yml

bootstrap_containers:
  - bootstrap

bootstrap_protocol_port: 40400

deployer_work_dir: ~/bot/deployer-bot
deployer_compose_file: deployer.yml

data_dirs:
  - ~/devnet-bootstrap/data
